# AtCoder for Kotlin

このリポジトリはAtCoderの設問をKotlinで解答したものをまとめたものです。

## プロジェクトの作成

設問ごとに新しいディレクトリを作成し、その中に`main.kt`ファイルを作成します。

```bash
mkdir ABC-XXX-X
cd ABC-XXX-X
touch main.kt
```

main.ktに解答コードを書きます。

## 実行方法

ディレクトリの中で以下のコマンドを実行してコードをコンパイルし、実行します。

```bash
kotlinc main.kt
kotlin.MainKt
```
